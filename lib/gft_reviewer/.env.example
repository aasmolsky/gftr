# Example environment variables for gft_reviewer
# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit real tokens.

# Environment: development | test | production (default)
# development/test: all MCPs skipped, LLM not called, returns rendered prompt
FRAI_ENV=development

# LLM (optional — if set, frai calls the API; if not set, CLI mode)
LLM_MODEL=
LLM_API_KEY=
