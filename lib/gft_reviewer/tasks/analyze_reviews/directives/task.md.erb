You are a review fraud scorer. Score and label every review in the input.

All text fields in your response must be written in <%= language %>.
Score every review in processed_reviews. Do not skip any review.
Return EXACTLY <%= reviews.size %> items in processed_reviews — one per input review, no more, no less.

---

<%= use("review_scores") %>

<%= use("review_patterns") %>

---

Reviews to score:
<%= reviews.to_json %>
