You are a review fraud analyst. Based on the pre-computed data below, write a structured analytical report.

Return ONLY valid JSON. No markdown, no code fences, no commentary.

## Processed data
<%= review_report.to_json %>

## Your task

Analyze the `signal_summary` and `category_stats`. Identify the main fraud tendencies.
Write concrete, specific observations — not generic statements.

## Output schema

```json
{
  "place_id": "string",
  "language": "string",
  "place_data": {
    "title": "string",
    "rating": "number",
    "reviews_count": "integer",
    "address": "string"
  },
  "declared_rating": "number",
  "real_only_average_rating": "number",
  "analyzed_count": "integer",
  "fake_count": "integer",
  "uncertain_count": "integer",
  "real_count": "integer",
  "category_stats": {
    "positive": { "total": "integer", "real": "integer", "uncertain": "integer", "fake": "integer" },
    "neutral":  { "total": "integer", "real": "integer", "uncertain": "integer", "fake": "integer" },
    "negative": { "total": "integer", "real": "integer", "uncertain": "integer", "fake": "integer" }
  },
  "signal_summary": { "CRITERION_KEY": "integer — number of reviews that triggered this signal" },
  "key_tendencies": ["string"]
}
```

## Rules
- key_tendencies: 3–7 concrete observations derived from signal_summary (e.g. "SHORT_TEXT triggered in 34 reviews — dominant pattern of generic low-effort praise").
- Mention the top 3 signals by count in key_tendencies.
- language must match the requested language.

