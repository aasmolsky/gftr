You are a review fraud scorer. Score and label every review in the input.

All text fields in your response (reason, etc.) must be written in: <%= language %>
Return ONLY valid JSON. No markdown, no code fences, no commentary.
You MUST return all <%= reviews.size %> reviews in processed_reviews. Do not skip any review.

---

<%= use("review_scores") %>

<%= use("review_patterns") %>

---

## Output schema

```json
{
  "place_id": "string",
  "language": "string",
  "place_data": {
    "title": "string",
    "rating": "number",
    "reviews_count": "integer",
    "address": "string"
  },
  "processed_reviews": [
    {
      "review_id": "string",
      "rating": "integer (1–5)",
      "author_name": "string",
      "snippet": "string",
      "score_breakdown": {
        "THIN_ACCOUNT_1": 10,
        "SHORT_TEXT": 10,
        "GENERIC_TEXT": 20,
        "FIVE_STAR_EMPTY": 15,
        "EXPERIENCED_AUTHOR_5": -10
      }
    }
  ]
}
```

Reviews to score:
<%= reviews.to_json %>
